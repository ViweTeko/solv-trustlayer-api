module.exports = {
    // Prettier config will NOT include printWidth: 80
    // because we are forcing ESLint's 'max-len' to handle it.
    tabWidth: 4,
    singleQuote: true,
    trailingComma: 'all',
    jsxSingleQuote: true,
    arrowParens: 'always',
  };